# Database Configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://YOUR_DB_HOST:3306/YOUR_DB_NAME?useSSL=true&requireSSL=true&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=YOUR_DB_USERNAME
spring.datasource.password=YOUR_DB_PASSWORD

# HikariCP connection pool settings (optimized for cloud DB)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.keepalive-time=120000
spring.datasource.hikari.leak-detection-threshold=60000

# Hibernate Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
spring.jpa.properties.hibernate.globally_quoted_identifiers_skip_column_definitions=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Initialize data (set to never after first run to avoid duplicates)
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=true

# REST API Configuration
spring.data.rest.base-path=/api

# Server Port
server.port=${PORT:8080}

#FRONTEND_URL=http://localhost:3001
#application.frontend.url=http://lo+calhost:3000
frontend.url=http://localhost:3000

server.error.include-message=never
server.error.include-stacktrace=never

# CORS allowed origins (comma-separated, override in prod via env var)
cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:5175,YOUR_PRODUCTION_FRONTEND_URL

# JWT Configuration
# IMPORTANT: Change this secret in production! Must be at least 256 bits (32 characters)
jwt.secret-key=${JWT_SECRET_KEY:MySecretKeyForJWTTokenGenerationAndValidation12345678}

# Virtual Threads (Java 21 feature for better concurrency)
spring.threads.virtual.enabled=true
